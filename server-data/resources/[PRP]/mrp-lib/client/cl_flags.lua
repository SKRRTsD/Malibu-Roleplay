function SetPedFlag(object, flag, enabled)
    exports['mrp-flags']:SetPedFlag(object, flag, enabled)
end

function HasPedFlag(object, flag)
    return exports['mrp-flags']:HasPedFlag(object, flag)
end

function GetPedFlags(object)
    return exports['mrp-flags']:GetPedFlags(object)
end

function SetPedFlags(object, flag)
    exports['mrp-flags']:SetPedFlags(object, flag)
end

function SetVehicleFlag(vehicle, flag, enabled)
    exports['mrp-flags']:SetVehicleFlag(vehicle, flag, enabled)
end

function HasVehicleFlag(vehicle, flag)
    return exports['mrp-flags']:HasVehicleFlag(vehicle, flag)
end

function GetVehicleFlags(vehicle)
    return exports['mrp-flags']:GetVehicleFlags(vehicle)
end

function SetVehicleFlags(object, flag)
    exports['mrp-flags']:SetVehicleFlags(object, flag)
end

function SetObjectFlag(object, flag, enabled)
    exports['mrp-flags']:SetObjectFlag(object, flag, enabled)
end

function HasObjectFlag(object, flag)
    return exports['mrp-flags']:HasObjectFlag(object, flag)
end

function GetObjectFlags(object)
    return exports['mrp-flags']:GetObjectFlags(object)
end

function SetObjectFlags(object, flag)
    exports['mrp-flags']:SetObjectFlags(object, flag)
end